<div class="container explore">
  <div class="search-users align-top">
    <div class="search-results-users py-5" data-controller="user-search">
      <%= form_with url: users_path, method: :get, html: { class: 'row g-2 mb-5 w-75', data: { user_search_target: 'form' } } do |f| %>
        <%= f.text_field :query,
                      class: "form-control",
                      placeholder: 'Search username',
                      value: params[:query],
                      data: {
                        user_search_target: 'input',
                        action: "keyup->user-search#search"
                      } %>
      <% end %>
      <%= render "list", users: @users %>
    </div>
  </div>
</div>
