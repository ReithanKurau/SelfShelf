<h1><%= @user.username %></h1>
<div class="search-results-users" data-controller="user-search">
  <%= form_with url: users_path, method: :get, html: { class: 'row g-2 justify-content-center mb-5', data: { user_search_target: 'form' } } do |f| %>
    <%= f.text_field :query,
                     class: "form-control",
                     placeholder: 'Find username',
                     value: params[:query],
                     data: {
                      user_search_target: 'input',
                      action: "keyup->user-search#search"
                    } %>
  <% end %>
  <%# <div data-user-search-target="list">
  </div> %>
  <%= render "list", users: @users %>
</div>
<%# <form class="row g-2 justify-content-center py-5" data-action="keyup->users#search">
    <input type="text"
          class="form-control"
          placeholder="Find username"
          data-users-target="input">
  </form>
  <ul id="esults"
      class="list-style"
      data-users-target="results">
  </ul> %>
</div>
<h3>Similar Shelves</h3>
<ul>
  <% @related_users.each do |u| %>
    <li><%= u.username %></li>
  <% end %>
</ul>
